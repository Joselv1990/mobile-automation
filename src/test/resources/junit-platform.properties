# Run Cucumber scenarios in parallel (one driver session per thread, see DriverManager).
# Disabled by default since a single emulator runs scenarios serially.
cucumber.execution.parallel.enabled=false
cucumber.execution.parallel.config.strategy=dynamic

# Make step/snippet output readable
cucumber.publish.quiet=true
cucumber.snippet-type=camelcase
